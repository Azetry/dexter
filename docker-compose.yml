services:
  dexter:
    build: .
    volumes:
      - .:/app
      - dexter_node_modules:/app/node_modules
    env_file:
      - .env
    stdin_open: true
    tty: true

volumes:
  dexter_node_modules:
